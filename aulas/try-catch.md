# Capturando Exceções em um Código Javascript

Um *exceção* é uma interrupção do fluxo de execução de um programa devido a uma instrução inválida. 

*Exemplo no Javascript:* 
Tornar um número inteiro maiúsculo:
```javascript
let valor = 12;
console.log(valor.toUpperCase());
```
